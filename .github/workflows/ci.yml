name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: archlinux

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: source PKGBUILD && pacman -Syu --noconfirm --needed "${makedepends[@]}" "${depends[@]}" base-devel
    - name: Create build user
      run: useradd -m build
    - name: Give permission for build directory
      run: chown build .
    - name: Build package
      run: su build -c makepkg
